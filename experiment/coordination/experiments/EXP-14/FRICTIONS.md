# EXP-14 frictions and repairs

## Fixed in the harness

| Friction | Observed consequence | Repair |
|---|---|---|
| Lead could not reply to the owner for Staff-owned Work | Owner mail stayed unread and the lead re-woke | Accountable lead may now persist the Work reply; live database regression added |
| Internal coordination turn had no owner reply id | Live activity could remain open | Complete the live turn with the recorded internal message id |
| OMP retried terminal quota failures internally | Terra produced repeated 18–25 second calls | Runtime profile disables retry and model fallback; Restless owns continuity |
| Bare `404 status code (no body)` was accepted as assistant prose | Failed route looked like `outcome_met` | Provider-content envelope classifier now treats it as transport failure |
| Runtime inbox help implied every inspection consumed mail | Operators could accidentally reason from the wrong semantics | Help now states own read consumes; `--as` inspection does not |
| Host OMP catalogue predated GLM 5.3 Flash | Exact route could not resolve | Upgraded and pinned OMP `18.0.10` |
| Host and company used different OMP generations | Native stream crashed as a JavaScript shape error and poisoned metering | Company image now pins the same OMP version as the host |
| Static custom model entries discarded `pi-native` transport | Runtime called a nonexistent raw route | Scoped proxy discovery plus exact model override preserves the native relay |
| GLM was advertised through the wrong protocol | Mixed Anthropic base plus Chat Completions returned misleading HTTP 200 | Use the BigModel coding-plan provider and Chat Completions route |
| HTTP status alone defined model admission | `{code,msg,success}` was mistaken for generated output | Require a valid completion/error envelope and exact route |
| Preserved Authority history referenced a deleted company | Daemon warned and touched the filesystem every five seconds | Reconcile only companies with a live config; preserved history remains dormant |
| Runtime launch omitted `DISPLAY` | First player client exited before a target existed | Launch propagates the admitted display and session-local writable homes |
| Bounded wait converted milliseconds incorrectly | Action duration contract was false | Unit conversion repaired and re-probed |
| Raw visual evidence accumulated | 62 PNGs survived across exploratory arms | Compact manifests retain observations and hashes; final cleanup deletes raw PNGs |

## Product and experiment frictions

| Friction | Treatment |
|---|---|
| Work named nonexistent branch `main`; seeded repository used `dev` | Superseded once with the observed exact ref. Future repository admission should return its default ref. |
| One player Work required both a clean negative and a full positive journey | Split into separate outcomes. A Work should have one terminal native claim. |
| Deterministic rejection proved unchanged state but not player-visible explanation | Fresh blind negative found the gap; one UI repair added actionable feedback. |
| Final negative player tested outside the destination zone and still claimed pass | Lead semantic review rejected the label. Correct in-zone final evidence remains owed. |
| Product repair changed the candidate after one successful blind journey | Acceptance restarted from zero. Pre-repair evidence remains diagnostic only. |
| GLM and GPT routes both exhausted capacity before final runs | Experiment stops blocked. Durable cooldown replaces retrying or silently changing model. |

## Remaining product questions

1. Can two different fresh players complete the exact final candidate without guidance?
2. Does the repaired panel appear for the intended in-zone route-zero condition, not only the
   outside-zone guard?
3. Is driving, camera feel and collision response enjoyable rather than merely functional?
4. Does the loop survive latency, packet loss and more than one client?
5. Can launch-to-handle transfer unchanged to browser and desktop applications before it becomes a
   universal Core primitive?
