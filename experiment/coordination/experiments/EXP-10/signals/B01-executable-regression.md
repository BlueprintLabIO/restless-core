# B01 - executable regression

**World:** controlled `_test` evaluation
**Source:** exact failing command and evaluator-labelled integration commit
**Source key:** `exp10:B01:parse-regression:v1`
**Target responsibility:** standing Swift Arrival Game Product lead

The evaluator has introduced one bounded parse regression into the isolated integration branch after
Cycle 1. Treat the failing `./validate-scripts.sh` output as authoritative. Preserve the accepted Cycle
1 product work, commission the smallest attributable repair, then require both:

1. the existing positive host/client delivery probe; and
2. a bounded negative probe proving that releasing the crate outside the destination does not complete
   delivery and exits cleanly on both host and client.

Do not rewrite the project or use elapsed time as success. Promote only an exact clean Staff commit
whose executable evidence supports both paths.
