# C-LL-01 — Prism Warden progression slice

Sparse ordinary-team crossover cell for a broad, tightly coupled coding/product outcome that was
predicted large but completed below one strong lead's effective-session saturation point.

- `scenario.md` is the frozen owner contract.
- `workload.md` records two independent pre-run structural judgements and the post-run correction.
- `evaluate.mjs` is frozen and hash-checked before either arm.
- `runs.md` records allocation, immutable arm evidence and the matched interpretation.
- `blind-review.md` records the seeded artifact-only quality comparison.

No wildcard mechanism is present. B0 is the accountable Sol lead alone. B1 is the same lead with one
ordinary Ox producer and one Staff slot.
