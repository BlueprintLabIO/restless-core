# C-LH-01 — field-research desk

Sparse ordinary-team crossover cell for a large, highly separable coding/product outcome.

- `scenario.md` is the frozen owner contract.
- `workload.md` records two independent pre-run structural judgements and the post-run correction.
- `evaluate.mjs` is the frozen external evaluator.
- `runs.md` records allocation and immutable arm evidence.

No wildcard mechanism is present. B0 is the accountable Sol lead alone. B1 is the same lead with one
ordinary free-model producer and one Staff slot.
