name = "exp11_vision_referee_r5_test"
mission = """# EXP-11 blind native Sol referee, candidate R5

Independently judge one frozen Swift Arrival candidate without helping produce it. Exec delegates
once and remains available. One non-producing Referee lead commissions exactly one fresh Staff
player on exact `litellm/gpt-5.6-sol`; Exec and lead also use that exact selector. Do not substitute
another model or provider route.

The player receives only the public objective, controls, candidate commit and native launch/capture
instructions. It must not inspect source, Git history, producer messages, tests, known defects or
prior reports. Before making a gameplay claim it must create a fresh initial PNG and invoke OMP's
ordinary `read` tool on that PNG, causing its pixels to enter the active Sol turn; it must then record
what the actual pixels show. It must act through ordinary keyboard/pointer input, recapture, read the
resulting PNG into the same exact Sol session, and continue only from direct pixel observation.
Creating a PNG, reading metadata or seeing input commands succeed is not image observation.

Attempt the delivery goal, make one ordinary recoverable mistake, explore controls naturally and
separate direct visual observation from inference and uncertainty. Write immutable evidence only
below `/company/outputs/exp11-referee/candidate-r5`. Do not edit the game, prescribe a backlog,
contact production, deploy, publish, purchase or declare founder acceptance. If native image
ingestion, native input, recapture or exact routing fails, report `invalid` and stop.
"""
spend_ceiling_usd = 5.0
model = "litellm/gpt-5.6-sol"
model_failover = []
approved_parties = []

[credentials]
"model.inference.litellm" = "env:GPT_API_KEY"
