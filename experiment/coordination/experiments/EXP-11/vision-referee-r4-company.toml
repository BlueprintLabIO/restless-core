name = "exp11_vision_referee_r4_test"
mission = """# EXP-11 blind native vision referee, candidate R4

Independently judge one frozen Swift Arrival candidate without helping produce it. Exec delegates
once and remains available. One non-producing Referee lead commissions exactly one fresh Staff
player on exact `litellm/gpt-5.6-sol`; Exec and lead also use exact
`litellm/gpt-5.6-sol`. This is the founder-authorised replacement after the requested Flash route
failed admission; do not substitute another model or provider route.

The player receives only the public objective, controls, candidate commit and native launch/capture
instructions. It must not inspect source, Git history, producer messages, tests, known defects or
prior reports. Before making a gameplay claim it must invoke the vision tool by writing its JSON
arguments to `xd://inspect_image`, describe the actual pixels of a fresh initial capture, act through
ordinary keyboard/pointer input, recapture, and invoke the vision tool again on the resulting image.
Creating a PNG, reading logs or reporting successful input commands is not image observation.

Attempt the delivery goal, make one ordinary recoverable mistake, explore controls naturally and
separate direct visual observation from inference and uncertainty. Write immutable evidence only
below `/company/outputs/exp11-referee/candidate-r4`. Do not edit the game, prescribe a backlog,
contact production, deploy, publish, purchase or declare founder acceptance. If vision ingestion,
native input, recapture or exact routing fails, report `invalid` and stop.
"""
spend_ceiling_usd = 8.0
model = "litellm/gpt-5.6-sol"
model_failover = []
approved_parties = []

[credentials]
"model.inference.litellm" = "env:GPT_API_KEY"
