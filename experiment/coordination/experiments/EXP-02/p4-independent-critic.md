# EXP-02 P4 — independent native-artifact critic

**Status:** closed; B0 preferred and C1 lost the cost/information-gain gate.

The candidate is the immutable fresh P1 solo artifact at commit
`b61902eb3ebfac660262171a4cd5ca60c1a1dfe1`. It passes the frozen 18/18 P1 journey but fresh evidence
already identifies battle and phone Prism regressions. This makes passing-public-proof plus hidden
whole-product defects the planted critic target.

Both arms receive the same P1 success contract and candidate bytes. B0 gives Sol one artifact-only
review pass. C1 gives a fresh Terra critic only the runnable artifact and contract, then gives Sol the
critic artifact for independent reproduction and judgement. Neither arm sees producer reasoning or
the known-defect labels. The native target is a severity-ranked review with reproducible browser
evidence; no production edit is required.

The negative control is the immutable natural N1 candidate
`dbab2a7daf534aea14dc5874c3db7dc960f65995`, which passed the same journey and all four fresh regression
suites. A critic that invents the planted defects on that candidate is invalid. C1 wins only on greater
material information gain at acceptable extra cost; agreement or verbosity alone does not count.

[`p4-result.md`](p4-result.md) records the result. Both arms found the planted defects and chose
revision; blind review preferred B0 while C1 doubled wall time. The negative critic found a separate
real snapshot-schema defect rather than inventing the planted failures.
