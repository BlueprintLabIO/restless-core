# EXP-02 P2 — one material interface change

**Status:** not activated. Neither A2 nor A3 reached its evidence gate.

P2 starts from the P1 scenario, seed and evaluator. After a real producer Attempt begins but before its
terminal artifact, the harness delivers this one immutable delta to both affected arms:

> The cavern must return a scene-derived `entryContext` object containing the exact source room and
> bridge state. Do not infer return state from a new global flag or duplicate the bridge lifecycle.

The lead must either answer/redirect the same Work or let the message stand if the original commission
already satisfies it. The producer must not be restarted merely because the interface changed.

The negative control delivers the delta after terminal artifact publication; the evaluator must flag
that delivery as too late even if the eventual candidate happens to pass. No A2/A3 run may change the
P1 owner outcome, model envelope or evaluator while testing the delivery mechanism.

Pre-run classification is near-saturation, bounded-interface coupling, stable module artifact becoming
moderately volatile at one known event, with high detection cost if the lead and producer silently
diverge. This is a communication screen, not an excuse to create a meeting cadence.
