# T02 — T1 coupled-product freeze

**Status:** frozen before model results
**Date:** 24 August 2026

T1 reuses the already classified small/high-coupling Perfect Guard cell so the new variable is
supervisor separation and model family, not a redesigned game task.

## Immutable inputs

- Seed commit: `514b7b3d0a65e093af608b08ca142344412181f4`.
- Scenario: [`C-SL-01/scenario.md`](../C-SL-01/scenario.md), SHA-256
  `44699970e702693657ecd59c66515eb804591446404167c8f896b3e5e275ab32`.
- External evaluator: [`C-SL-01/evaluate.mjs`](../C-SL-01/evaluate.mjs), SHA-256
  `c2e18bbae886b84d955844ae16b837e6a008d7a22578040a5392380be3f12c19`.
- Native result: playable browser battle plus evaluator's 11 observable checks.
- Seed negative control expected: 7/11; the absent outcome is discoverability, late-hit negation,
  exact +18 energy and visible Perfect Guard feedback.

## Matched organisations

| Arm | Exact shape |
| --- | --- |
| **S1-T** | `supervisor-lead` on `zai/glm-5.3` supervises one end-to-end `gameplay-systems` worker on the same model; terminal/block events only; lead performs no production |
| **C0** | One direct `studio-lead` on `zai/glm-5.3`; no Staff; diagnostic counterfactual only |

Both arms use `high` reasoning, the same Company Runtime, tools, source tree, native review path,
scenario and evaluator. Actor completion is callback/process exit; the 1,800-second run envelope is a
budget, not semantic timeout. Per-arm spend ceiling is US$8.

## Frozen order

Order seed: `EXP-03:T1:GLM-5.3:supervisor-premium:2026-08-24`
SHA-256: `8045ec59c7e22a3e495986818bd218a102993e65d706143ce1fcefb052ef341e`

Even first byte selects **S1-T → C0**.

Run IDs:

- `exp03-t1-s1t-glm53-r3`
- `exp03-t1-c0-glm53-r1`

### Preflight amendment

`r1` and `r2` are excluded infrastructure preflights, not repetitions. Both launch commands omitted
the frozen scenario path and therefore prepared the broader company mission (observed SHA-256
`a81b6171abd6c7ba37b8b2145f3c1a10ed9166769ec3c62aee2158a8d6f48808`). `r1` additionally exposed
and led to a fix for qualified-provider rewriting at the worker boundary. `r2` was stopped before it
commissioned Work. Neither can answer the experiment question.

Before counted `r3`, prepare must receive the scenario and evaluator files plus their two expected
hashes. The harness now rejects a mismatch before the run can start. This amendment changes no task,
prompt, evaluator, model, effort, organisation or repair rule; it only makes the frozen bytes
mechanically mandatory.

## Counted interpretation

- Outcome gate decides acceptance; model claims do not.
- S1-T is valid only with one produced worker Work, exact artifact promotion, a clean candidate and no
  lead-authored final content.
- C0 is valid only with zero Work below the direct actor.
- Provider or harness failure before useful cognition is infrastructure-invalid.
- A C0 speed win prices the supervisory invariant; it cannot remove it from product architecture.
- No prompt, scenario, evaluator, model, effort or repair rule changes after the first arm starts.
