# T01 — Supervisor-only conformance

**Disposition:** passed 16/16
**Date:** 24 August 2026
**Evidence class:** deterministic harness/architecture proof; no model call

Run `exp03-supervisor-architecture-r3` proved the new EXP-03 manipulation before counted
coordination cell.

## Proved boundaries

- `supervisor-lead` and `gameplay-systems` are distinct attributable actors.
- Lead and worker model slots can be frozen to the same GLM-5.3 identity.
- An already-qualified `zai/glm-5.3` selector remains direct Z.ai at the worker boundary.
- The supervisor has exactly one Staff slot in S1.
- The supervisor prompt forbids production, private parallel work, content-changing integration,
  polling and timeout inference.
- Narrating supervision without real Work is rejected.
- Exactly one whole-outcome responsibility can be commissioned to the worker.
- The worker receives the exact responsibility, native runtime capability and terminal artifact
  contract.
- A terminal report is verified against the declared gate and imports the exact worker commit.
- The candidate becomes that exact commit through mechanical promotion.
- The final candidate is clean and has no lead-authored content difference.
- The supervisor owns no production Work.
- Worker completion emits an event wake containing exact artifact facts.
- The coordination database remains healthy.

The raw 16-check result remains in the isolated ignored run at
`v2/workdir/exp03-supervisor-architecture-r3/supervisor-architecture-results.json`.

## Limit

This proves the topology is mechanically expressible and truthfully observable. It does not prove a
GLM-5.3 supervisor will follow the contract, brief well, judge well or outperform a counterfactual.
Those are counted-run questions.
