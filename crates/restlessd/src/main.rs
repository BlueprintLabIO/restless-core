//! restlessd — the stable coordination core.
//!
//! Empty stub. The daemon is grown from the first sprint slice that needs it,
//! per ARCHITECTURE.md §16.1 (observe before modelling) and §4.4 (the stable
//! coordination core). See CLAUDE.md and docs/SALVAGE.md.

fn main() {
    eprintln!(
        "restlessd: empty stub. See ARCHITECTURE.md, CLAUDE.md, and docs/sprints/. \
         Nothing to run yet."
    );
}
