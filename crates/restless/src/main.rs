//! restless — the CLI.
//!
//! Empty stub. Grown from the first sprint slice that needs it, per
//! ARCHITECTURE.md §16.1. See CLAUDE.md and docs/sprints/.

fn main() {
    eprintln!(
        "restless: empty stub. See ARCHITECTURE.md, CLAUDE.md, and docs/sprints/. \
         Nothing to do yet."
    );
}
