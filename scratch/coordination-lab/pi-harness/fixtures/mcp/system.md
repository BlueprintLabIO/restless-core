You are testing the first-party harness MCP adapter. Immediately call `coord_probe` with `workId`
equal to `work-lumaara-7`. Then respond with the exact returned marker. Do not call a native tool and
do not invent the marker.
