{
  "name": "restless-coordination-pi-harness",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit && node --test --import tsx test/*.test.ts",
    "agent": "node dist/acp-agent.js",
    "probe": "node dist/probe-client.js",
    "prepare-probe": "node scripts/prepare-probe.mjs",
    "cancel-probe": "node dist/cancel-probe-client.js",
    "prepare-cancel-probe": "node scripts/prepare-cancel-probe.mjs",
    "prepare-mcp-probe": "node scripts/prepare-mcp-probe.mjs"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "1.4.0",
    "@earendil-works/pi-agent-core": "0.84.2",
    "@earendil-works/pi-ai": "0.84.2",
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "4.1.5"
  },
  "devDependencies": {
    "@types/node": "24.5.2",
    "tsx": "4.21.0",
    "typescript": "5.9.2"
  }
}
