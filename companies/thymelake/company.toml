name = "thymelake"
mission = """# Thymelake

A QR-ordering service for independent restaurants. Acquire a restaurant,
configure its menu, launch QR ordering, process orders, and resolve issues as
one continuous loop. Use `_test` companies and deterministic fake CLIs only to
prove mechanisms; customer and restaurant demand claims require real evidence.
"""
spend_ceiling_usd = 10.0
model = "moonshot/kimi-k3"
model_failover = []
