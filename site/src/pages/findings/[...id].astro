---
export function getStaticPaths() {
  return [
    'teams-need-a-crossover',
    'supervision-needs-to-stay-available',
    'four-departments-one-invalid-evaluator',
    'work-graph-is-a-record'
  ].map((id) => ({ params: { id } }));
}

return Astro.redirect('/journal/', 301);
---
