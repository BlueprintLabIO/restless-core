{
  "name": "restless-publication",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "verify": "npm run build && node scripts/verify.mjs"
  },
  "devDependencies": {
    "@fontsource-variable/archivo": "^5.2.8",
    "@fontsource-variable/newsreader": "^5.2.8",
    "vite": "^7.1.3"
  }
}
