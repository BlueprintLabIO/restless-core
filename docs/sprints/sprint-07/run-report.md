# Sprint 07 run report

**Run date:** 19 August 2026  
**Companies:** `sprint07_test` for scripted shapes; `aris` for genuine work  
**Current status:** T1–T4 are implemented and verified. The genuine Aris release-integrity chain has
produced one current, lead-authored owner item through the admission path. T5 remains open only for
the founder's ten-second comprehension check. This report does not convert that human observation
into a proxy.

## Baseline

The founder-supplied first-viewport capture showed the genuine Aris release-integrity item before
Sprint 07. The owner had to reconstruct one simple recovery story from the following visible material:

- an internal Work title containing Terms/Privacy, legal audit, sitemap DB decoupling, `/api/health`,
  commit exposure and the superseded Work identifier;
- worktree paths, commit hashes, Attempt identifiers and the validation-chain topology;
- the same disk-space diagnosis and recommendation repeated in the detail and Exec rail;
- a generic review action even though the immediate state was an operational blocker;
- implementation detail visible before any evidence disclosure.

The concise source-supported account was: the release work was prepared, the host had about
300 MiB free, at least 10 GiB was needed, and the company would resume after the platform condition
cleared. The founder's observed friction was that the screen was too implementation-oriented and
cluttered to present to a CEO. No baseline time-to-comprehension number was recorded, so the final
run will compare required reading, disclosures and clarification rather than inventing a duration.

## Landed boundary

- The existing OrgIntel owner handoff now carries one compact authored presentation payload plus
  author, authored time and a deterministic source fingerprint. Work, Attempts, artifacts and gates
  remain the operational/evidence truth.
- One `present-to-owner` skill source is included in both accountable-lead and Exec context. It tells
  the actor to finish machine-doable work, classify the real owner need, separate observation from
  recommendation, preserve uncertainty and ask for the smallest exact action.
- Team members route ordinary judgement to their lead; lead-owned judgement routes to Exec; Exec
  must explicitly admit a current authored brief to the owner. Exec-authored judgement is not
  self-admitting. The five irreducible human categories keep their direct boundary and factual fallback.
- The attention projection maps authored meaning unchanged. It no longer reuses the Work title,
  invents generic “why/recommendation” copy or treats every judgement as an outcome review.
- The source kind selects truthful actions: review acceptance/revision, a recorded bounded decision,
  or a prepared human step. Conversation stays pending until an explicit source-owned transition.
- The cockpit uses one editorial decision folio, a scannable title-plus-ask queue, collapsed source
  evidence and one persistent conversation with the accountable lead. The rail no longer repeats a
  synthetic copy of the selected brief.

The admission gate is deliberately split across existing responsibilities rather than implemented as
a presentation service. The accountable lead authors the meaning after the work and independent
evidence are prepared. Exec checks that the brief is current, supported, consequential and names an
irreducible owner need; it may resolve or return work that has not earned owner attention. The owner
projection then maps only an admitted current brief. It does not rewrite, summarise or improve it.

## Scripted `_test` run

The run used `sprint07_test`, never live Aris, for synthetic facts.

### Outcome review

- Work: `70000000-0000-4000-8000-000000000001`
- Handoff: `71000000-0000-4000-8000-000000000001`
- Accountable author: `offer-lead`
- Initial fingerprint: `8fff59e203b513bd32615d0cd0906cf1446d54c01c564923f46fa21be24a8b54`
- Refreshed fingerprint: `9db9ddfaf48ace1fd551dda4b05aedd627e1e2ea6b506fe40447a700c21f6fdf`

The first scripted artifact used a placeholder URL and referenced a critic file that did not exist.
The independent critic correctly rejected that evidence chain. The fixture was then repaired rather
than blessed: a supervised loopback service now renders the exact scripted outcome at
`http://127.0.0.1:4173/`, and `/company/outputs/centre-offer-critic.md` states precisely what the
test page proves and does not prove. The material handoff refresh immediately removed the stale brief
from Attention, reassigned the row to Exec, required an attributed lead refresh and then required an
explicit Exec admission. The old brief remained historical but could not render as current.

The browser opened the target through the isolated review origin and exposed the real page inside the
main review surface. `Hold to accept` appeared in the rail header immediately left of the icon-only
back control. The lead conversation remained available below it.

### Bounded decision

- Work: `70000000-0000-4000-8000-000000000002`
- Handoff: `71000000-0000-4000-8000-000000000002`
- Author: `offer-lead`
- Fingerprint: `bf973ef9db552dfef627f6500e0cb3388cc01ac93ac0866e395b928e4acaee14`

The first viewport showed the two-versus-ten-centre choice, impact, recommendation, uncertainty,
deadline, no-action consequence and a text field labelled `Your decision`. It did not show a false
review or browser action. Opening `Talk to Offer lead` changed only the rail; the handoff remained
pending. Recording `Proceed with the two-centre pilot.` invoked the handoff decision endpoint, stored
that exact resolution, removed the item from Attention, changed the Work from blocked to active and
returned the answer as input to the next Attempt.

### Irreducible human step

- Work: `70000000-0000-4000-8000-000000000003`
- Handoff: `71000000-0000-4000-8000-000000000003`
- Category: `mfa`

The item rendered the honest direct-human fallback: confirm the sign-in, enter the six-digit code in
the prepared billing session, and allow the company to observe authentication. It used `Open prepared
browser`, not review or decision semantics. The accountable team lead was recovered from the Work
owner's team for discussion; no anonymous summary was invented.

### Machine-doable non-item

- Work: `a09307f9-891c-47d6-8d43-8364de7d6ba1`
- Handoff: `4fbc39ac-35fc-41c6-945d-209bf67465ce`

The maker asked for judgement on regenerating an “already-produced” thumbnail. The request routed to
`offer-lead` and never appeared in owner Attention. The first lead turn withdrew it with a claim that
source and thumbnail digests already matched. A later failover-backed lead audit searched the company
computer and found no source image or thumbnail at all, so that resolution had no observable
referent. The lead preserved the withdrawn handoff as history, abandoned the stale Work and removed
its dead `requires` edge. This case therefore proves below-owner correction and non-admission, but it
is not counted as a successful machine-verification fixture. The genuine Aris storage recovery is the
valid locally resolved non-item for criterion 4.

## Independent briefing review

Independent critic Work `53d581b8-5936-4104-b079-845649081e65` completed and recorded
`/company/outputs/sprint07-owner-brief-critic.md` as artifact
`55434b44-ec68-48b7-92b9-ccbee2c39eab`. It found that both executive stories were clear, bounded and
appropriately uncertain, but it refused the first outcome's placeholder target and missing critic
file and identified the decision fixture's unlinked synthetic commercial inputs. These are recorded
limitations, not silently promoted claims.

A second independent Work, `a61950dd-e16b-4ddd-ad24-c2f8db3ecde0`, wrote
`/company/outputs/sprint07-owner-brief-critic-final.md` but did not record the required artifact and
therefore ended blocked. Its report also inspected historic Exec prose instead of the current handoff,
so it is not accepted as current-brief evidence. A third, more tightly bounded Work,
`4939f0e1-ec38-4dbe-8eaa-67299f8e65a6`, was then commissioned with the exact handoff, native target,
source report and artifact-recording instruction. The runtime refused that attempt because every
configured `_test` model candidate was in provider quota cooldown. After the already-live-probed Kimi
route was added as the test company's failover, attempt
`980e5def-5821-4a99-b75f-476e9978148b` inspected the exact current graph row, fetched the loopback
target, read the linked source report and returned **ACCEPT**. It recorded
`/company/outputs/sprint07-owner-brief-critic-current.md` as artifact
`19b0622b-574c-460d-90f4-28fc13c0fad2`. The report supports each claim individually, preserves two
explicit caveats and confirms the ask is understandable without implementation detail. No
human-authored or stale report was substituted during the cooldown.

## Browser and restart verification

At a 1600×1000 viewport, the accessibility tree and rendered capture showed:

- queue rows with complete, distinguishable owner headlines and short asks;
- one reading order: headline → situation → impact/uncertainty → recommendation → next move;
- source author and freshness visible without opening evidence;
- raw prepared notes and identifiers absent until `Evidence and source detail` was expanded;
- decision and review affordances chosen from backend `actions`, not inferred from runtime presence;
- `Talk to Offer lead` opening the responsible-lead rail with the short `Message Offer lead`
  placeholder and no duplicated brief;
- the real loopback test outcome in the main surface, with the hold-to-accept and icon-back controls
  in the rail header.

At a 390×844 emulated mobile viewport, the first pass exposed both a min-content page overflow and a
top-bar collision. The root grid now constrains its only track with `minmax(0, 1fr)`; the active owner
surface keeps its label while inactive surfaces collapse to their marks with explicit accessible
labels. The observed document width and scroll width were both 390 px, and the queue and focused
folio remained in the viewport. Opening a lead conversation then exposed a second defect: the old
compact breakpoint hid the rail entirely. It now becomes a bounded 382 px overlay below the top bar,
with no document overflow. The measured review controls were `Hold to accept` at x=216 followed by
the 34 px icon-only back control at x=337; the 279 px `Message the lead…` composer remained anchored
at the bottom. A Lighthouse snapshot of the underlying mobile state scored 100 for accessibility and
100 for best practices; search-crawler metadata findings are not release criteria for this local
authenticated control surface.

Dogfood also exposed an admission seam: a lead could replace the content of an already owner-visible
brief without asking Exec to admit the changed meaning. Re-authoring an admitted ordinary judgement
now atomically returns it to Exec, records the retraction, removes it from owner Attention and requires
a fresh explicit admission. The database scenario exercises the complete prepare → admit → re-author
→ hide → re-admit sequence. The same sequence was then exercised through the real CLI and owner API
in `sprint07_test`: the refreshed review count fell to zero, the row was owed by Exec, and only the
subsequent attributed Exec escalation restored its exact revised copy and four source-backed actions.

After rebuilding the company image, replacing the Aris container while preserving its volume and
restarting the host stack, `doctor aris` returned `live`: runtime reconciliation current, all six
supervised services running, browser available, owner API and cockpit available, and 199 GiB host
storage free. The review brief retained its author and exact persisted copy across the restart.

## Genuine Aris run

Migration 0009 returned two pre-Sprint-07 ordinary judgements to Exec instead of rendering their raw
`prepared_state` to the owner. The first genuine wake withdrew the already-answered centre-offer
handoff and exposed that the prior disk conclusion had not been observable from inside the company
container. No item reached Attention.

The operator-side probe then established 199 GiB free and a live reconciled runtime. A second genuine
Exec wake used that observed platform evidence, resumed release-integrity producer Work
`9ed83c5d-39d2-459a-8967-2b08e9c26b61`, and left the downstream gates, render validation,
independent critic and lead-acceptance nodes to run below the owner. This is the required negative
case in live company state: an operational condition that the company could continue from did not
become a beautified owner interruption.

The chain then exercised revision rather than promoting a convenient green state. Revision 1
produced the legal, sitemap and health changes, but independent review found an hreflang defect.
Revision 2 repaired that defect and passed its gates. Render validation at 390, 768 and 1440 px
proved all twelve interactive tabs, the expected image and PDF responses, no horizontal overflow and
no console or page errors; it also found that the legal pages still shared an Open Graph image. The
critic's prose requested changes, but its terminal decision incorrectly said `outcome_met`; the lead
nevertheless rejected the candidate and created no owner handoff. This mismatch is retained as a
coordination defect rather than treated as acceptance evidence.

A narrow graph repair returned the canonical chain to revision 3. A stale Exec conversation also
created a duplicate five-node validation chain after the canonical repair. Those nodes were
abandoned, their actors stopped, and three coordination reports they had committed to the product
branch root were moved back to `/company/outputs`. The canonical producer's product revision
completed at `a7b5b0e3ccd8511afb5098d678942205dd20bdac`; its cleanup-stable tip is
`7f780fc4ef02385d733bb38034a4c8dc825676e1`, with zero product diff from that revision. Its product
tree is identical to the validated `a0caa880cfd3ff32d9354eb85963acfc213c53ee` revision and the final
worktree is clean. The revision adds page-specific Terms and Privacy Open Graph images, removes an
accidental screenshot, preserves the accepted hreflang repair and keeps `/api/health` honest when no
build commit was supplied.

This run also found that completion messages can repeatedly start free-form lead and Exec
conversations before ready Work is claimed. Those conversations consumed the accountable actor,
read stale plans and nearly recreated the retired chain. Their inbox rows were drained, the stale
turns were stopped and the exact gate node was resumed after model transport cooldown; the Work and
evidence history were preserved. This is an observed scheduler-priority defect, not a reason to
short-circuit independent validation.

The repaired canonical gate completed at the final tip. Fresh render-validation Work
`1bcce40c-c0ad-42e9-a1a8-4932fe160720` passed the live target, and fresh independent-critic Work
`7ef1f4f7-8129-4080-a735-9d154033bb43` returned **ACCEPT** against that exact candidate. The
accountable lead then reran the full gates, live-probed the prepared target, wrote
`/company/outputs/aris-tutoring-centres/sprint-07/lead-verdict.md`, returned **ACCEPT**, and created
exactly one owner handoff: `361a772c-0624-462c-ba41-58a1a554e4d1`.

That handoff exposed a terminal-semantics bug in the admission path. Exec resolved the still-unbriefed
request with contradictory prose: it claimed Exec approval while also saying the item was ready for
the owner's decision. Resolution correctly removed the row from Attention, so the apparent empty
queue was truthful to state but wrong for the intended outcome. Duplicate retry attempts were
stopped; the same handoff row was restored rather than cloning a second request, its repair was
recorded, and the lead authored a current brief before Exec admitted it. Exec context now receives the
brief's authored meaning and explicitly treats resolution as terminal; the shared prompt requires an
ordinary judgement handoff to be briefed in the same turn and forbids resolving with prose that still
asks the owner to decide.

The admitted item is now the only Aris Attention row. Its headline is “The Aris centre offer is ready
to publish”; it explains the legal/discoverability outcome, recommends approval, states that no
production change or outreach occurs without the owner decision, preserves the deployment
`BUILD_COMMIT` uncertainty and attributes the brief to `site-validation-lead`. The supervised native
target is live at `http://127.0.0.1:4173/for-tutoring-centres` inside the company runtime and projects
as `available`. A punctuation defect discovered while extracting that URL from prepared prose was
also fixed and covered: sentence-ending semicolons no longer become part of the route.

The Work remains blocked on the owner by design. No implementation or verification step accepted or
rejected the outcome on the founder's behalf.

## Deterministic verification

- `cargo test --workspace`: 89 tests passed (2 CLI, 12 model-gateway, 1 actors/teams, 1 bindings,
  1 escalation, 8 OrgIntel smoke, 64 daemon; doc tests empty).
- `RESTLESS_TEST_DATABASE_URL=postgresql:///restless cargo test -p restless-orgintel --test
  escalation --test smoke --test actors_and_teams -- --nocapture`: 10 database scenarios passed.
- `npm run check`: zero Svelte errors and warnings; raw-size type guard passed.
- `npm run build`: static production build completed.
- `git diff --check`: clean.
- Browser action check: exact decision persisted on the handoff and released its Work.
- Restart check: Aris was reconciled to the current image with its company volume preserved;
  `doctor aris` and `doctor sprint07_test` both returned `live` with no actions. The Aris owner API
  still returned exactly one current handoff and an available native review target after replacement.

## Deletion report

Removed from the selected representation:

- generic OrgIntel owner-judgement summary, “why it matters” and recommendation prose invented by
  the attention projection;
- internal Work-title reuse as the owner headline;
- raw `prepared_state` as primary owner reading material;
- the equal-weight four-fact grid;
- generic review semantics for decisions and blockers;
- the selected-item pseudo-message/context block in the conversation rail;
- the separate bottom decision panel and long composer placeholder.

Preserved deliberately: Work history, prepared source notes, commits, artifacts, gates, critic
findings, receipts and actual conversation messages. The sprint changes selection and prominence,
not evidence custody.

## Criterion audit

| # | Result | Evidence |
|---|---|---|
| 1 | **Pending human observation** | The final folio contains all five required facts without evidence expansion; only the founder can supply the ten-second account. |
| 2 | Pass | Both scripted organisational briefs persist `offer-lead`, authored time and source fingerprint. |
| 3 | Pass | One prompt source is included in lead and Exec context; no BFF/browser model call or presentation service exists. |
| 4 | Pass | Genuine Aris storage recovery resolved below owner and never entered Attention; the flawed `_test` thumbnail claim was later corrected and retired below owner rather than promoted. |
| 5 | Pass | Review, decision and MFA rendered different actions. Database dogfood exercised both review outcomes: exact revision feedback invalidated the Work graph and became next-Attempt input; acceptance completed the reviewed Work. The owner HTTP route maps only those two decisions to that OrgIntel transition, and conversation did not resolve either handoff. |
| 6 | Pass | One folio owns the prepared story; rail copy is identity, durable conversation and composer only. |
| 7 | Pass with fixture caveat recorded | Raw evidence is disclosure-only and complete for the repaired review target; the critic explicitly rejected unsupported synthetic decision inputs. |
| 8 | Pass | The supervised loopback page opened as the primary review surface through the isolated review gateway. |
| 9 | Pass | Restart preserved exact copy/author; material refresh hid stale meaning and required attributed re-preparation/admission. |
| 10 | Pass | Ordinary missing/malformed briefs are withheld; direct human categories use a factual fallback. |
| 11 | Pass | Browser tree showed two narrow queue rows distinguishable by full title and ask. |
| 12 | **Pending final human comparison** | The genuine Aris view requires zero technical disclosure to find the ask and shows no duplicate brief; time/clarification outcome awaits the founder's account. |

## Remaining friction

- The initial scripted fixture proved that a linked artifact row is not the same as a reachable
  outcome. The final run must continue to probe the target, not trust metadata.
- The independent critic correctly needed source files, not just confident summary prose. A future
  run may prove that evidence availability deserves an explicit live health state in the projection;
  this sprint does not add speculative evidence custody or a new lifecycle.
- Historic Exec messages remain historic and can contain old implementation-heavy prose. The selected
  brief no longer duplicates them, but history is not rewritten.
- The genuine run proved that grammatically plausible Exec prose can contradict its state transition.
  Context and prompt guidance now name the terminal semantics, but a future sprint should decide
  whether resolution/request contradiction warrants a deterministic invariant after more examples.
- Human comprehension and taste cannot be replaced by an automated score. They remain the explicit
  final founder observation rather than a fake green check.
- Provider quota cooldown can delay an independent actor even when the source, target and Work are
  correctly prepared. The run preserved the failed attempt and resumed on a separately live-probed
  configured failover; it did not let Exec, the UI or the implementer self-certify the brief.
- A reviewer can currently write `CHANGES_REQUESTED` in its artifact while returning
  `outcome_met` in the runtime decision envelope. The lead caught this run, but the two signals need
  one semantic invariant before another sprint relies on automatic downstream release.
- Actor-addressed completion mail currently outranks ready Work and can repeatedly occupy the same
  lead with coordination turns. That is useful for real judgement, but ordinary progress notices
  need coalescing or a lower scheduling priority so they cannot starve the graph they describe.
- Runtime launch refusals currently consume the same attempt budget as an actor that actually ran.
  Three transport-cooldown refusals exhausted the canonical gate node without executing one command,
  so the run had to preserve and rewire its evidence through a replacement node. Launch admission
  should happen before an Attempt spends its retry allowance.
- Validation actors committed evidence reports to the product branch when their prompt did not
  make the `/company/outputs` boundary sufficiently explicit. The debris was removed in this run;
  future review prompts should name the product tree as read-only unless remediation is the Work.
