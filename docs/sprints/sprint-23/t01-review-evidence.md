# S23-T1 — Extend web review for design-language evidence

**Layer:** Company Runtime + evaluation.

Keep mechanical capture factual, but package paired product/candidate screenshots, computed typography,
colour, width and motion observations so a critic can inspect fidelity. Do not invent a similarity score.

**Observed friction:** Sprint 22 proved absence of browser defects but not fidelity to Restless.

**Deletion target:** clean-manifest-as-design-acceptance claims.

## Observed closure — 28 August 2026

- `restless-web-review` schema v2 records computed root style, typography frequency, text/background/
  border palette, radii, backdrop use, animation/transition vocabulary, reading-surface dimensions and
  SVG/canvas/image/video/authored-visual counts.
- `--reference-route` permits multiple native states when the reference is browser-addressable; static
  product captures remain valid critic evidence when the loopback owner surface is intentionally not
  reachable from a company container.
- The installed skill now requires product-first comparison and explicitly rejects invented marketing
  identities and mechanical-cleanliness acceptance.
- Live baseline probe `/company/outputs/s23-v2-tool-probe/manifest.json` proves the rejected Blog body is
  720px wide with a 201.594px left indent, cream root background and zero recorded visual elements at
  desktop; the same route is mechanically healthy. The tool reports the conflict without scoring it.
- The company image rebuilt successfully with the v2 tool and revised skill; the active Sprint 23
  company received the exact updated files without disrupting its running revision.
