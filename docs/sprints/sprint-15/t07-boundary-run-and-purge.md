# S15-T7 — Run the boundary scenario, purge and report

**Layer:** All three planes plus evaluation.

**Observed friction served:** security and type changes can compile while a Runtime loses ordinary
coordination, a model relay loses streaming behavior, or a stale explanation still claims an unsafe
topology is acceptable.

## Outcome

A throwaway-company boundary run proves refusal and ordinary operation together; superseded comments,
duplicate meters and stale sprint claims are removed.

## Acceptance

- Run the full Sprint 15 boundary evidence package against a `_test` company only.
- Record exact inputs and observed refusal/success outputs in the run report.
- Run format, strict Clippy, full workspace tests, web checks, live-Postgres verifier and `restless
  doctor -c <test company>` when the configured environment permits them.
- Delete implementation paths and documentation made obsolete by the selected capability canon.

## Deletion target

Accepted-risk comments, duplicated charged usage and stale “green means live DB” claims.
