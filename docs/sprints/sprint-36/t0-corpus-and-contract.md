# S36-T0 — Freeze the publication corpus and Core/Cloud contract

**Layer:** Cross-layer contract and evaluation

**Observed outcome or friction:** A URL can look successful while pointing at a mutable process,
exposing a Runtime or lacking a recoverable service identity.

**Work:** Freeze one interactive HTTPS/WebSocket demo, one Godot ENet/UDP server, exact manifests,
client probes and adversarial cases for mutable tags, digest mismatch, cross-company access, expired
invitation, duplicate provider reply, leaked port, missing cleanup and Runtime-public routing. Publish
versioned request, invitation, observation and terminal-receipt fixtures for Cloud consumption.

**Evidence:** Fixtures hash before implementation; Core and Cloud decode the same golden messages; the
negative corpus fails for the named reason rather than generic parsing.

**Deletion target:** Prose-only integration assumptions and provider-specific payloads presented as the
Core contract.
