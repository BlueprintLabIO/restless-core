# S17-T6 — Run the real-provider fault matrix, purge and report

**Outcome:** A dedicated real-provider `_test` run and fault matrix prove the complete signal-ready
supervised loop; all losing scaffolding is removed or clearly quarantined.

**Layer:** Full product slice and evaluation.

**Observed friction served:** Static checks and simulated live-company inputs have repeatedly been
mistaken for proof.

**Acceptance:** Observe one real signed inbound event through Authority, correlation, lead, worker and
prepared result; run duplicate/out-of-order, projection restart, load failure, missing tools,
no-callback, injection, active-Work and provider-degraded controls; prove refusal of outbound send
unless separately authorised; record latency, wakes, duplication, usage and harmful-tail results.

**Deletion:** File polling, global coalescing, empty thought floods, duplicate ingress/session paths and
any abstraction not used by the real run.

**Evidence:** `docs/sprints/sprint-17/evidence/final-report.md` plus raw bounded receipts and exact
verification commands.
