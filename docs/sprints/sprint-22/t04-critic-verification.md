# S22-T4 - Critique, revise and verify the exact candidate

**Layer:** OrgIntel + Company Runtime.

Give the fresh critic the exact live site, generated captures, ground-truth dossier and outcome contract.
Return one consolidated consequential revision packet, let the producer repair it, then rerun clean
build, route, link, browser-layout and supervised-service checks.

**Observed friction:** screenshots existed without being judged, and source/build checks could not see
headline collision, collapsed graphics or invisible content.

**Deletion target:** same-context approval and source-only visual review.

## Observed closure — 28 August 2026

- Producer review manifest `/company/outputs/work-6d31ba3a4a77-r1-web-review/manifest.json` records 45
  candidate states plus two reference states, all HTTP 200 with no overflow, off-viewport interactive
  element, invisible authored content, failed internal link, console error or page error.
- The producer repaired seven mobile Corpus status links found by its first complete capture before
  submitting the exact commit.
- The critic independently captured the live site, retried Terms separately and keyboard-probed focus.
  Its verdict was `accept`; the 10px direct-navigation labels were recorded as a bounded concern rather
  than silently ignored.
- The non-producing lead's second manifest at
  `/company/outputs/work-fc1665bc57b0-r1-web-review/manifest.json` repeated the clean 45-state result.
- Host-side `npm run verify:portable` passed the corpus and quality gates, Astro diagnostics (24 files,
  zero errors/warnings/hints) and a 21-page static build at the exact final commit.
