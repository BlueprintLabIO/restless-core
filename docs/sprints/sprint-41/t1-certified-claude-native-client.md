# S41-T1 — Add the certified Claude native client

**Layer:** Runtime image + Runtime Bridge + model gateway  
**Serves:** The owner needs the real Claude Code controls without escaping the admitted build or auth boundary.

## Work

- Package or expose the genuine Claude Code CLI build already identified by the Sprint 39 adapter and
  report its exact runtime identity.
- Launch it with isolated `CLAUDE_CONFIG_DIR`, exact cwd, model, effort, permission mode, approved tools
  and MCP servers.
- Route provider access through the existing scoped host relay without delivering a reusable provider
  or subscription credential to Runtime.
- Separate interactive-only capabilities from the certified autonomous profile and visibly refuse
  unsupported personal configuration.
- Add readiness, auth, protocol and cleanup diagnostics before native input.

## Acceptance

Launch inspection proves the exact binary, policy, environment-name-only credential contract and
workspace. Missing relay, wrong model/build or ambient config fails before productive input, and exit
leaves no secret or owned process residue.

## Makes deletable

Developer Claude installations, raw-key native experiments and mutable `latest` resolution.
