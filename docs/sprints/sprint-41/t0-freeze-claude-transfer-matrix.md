# S41-T0 — Freeze the Claude transfer matrix

**Layer:** Runtime evaluation  
**Serves:** Shared engine and session terminology do not prove that SDK, ACP, CLI and Desktop can share
one session under Restless's authentication route.

## Work

- Pin the counted adapter, Agent SDK, Claude Code CLI, model, Node runtime and session-storage builds.
- Probe live attachment, stored-session resume, continue, fork and new-session reconstruction between
  the ACP/SDK path and genuine CLI.
- Record authentication compatibility for the scoped relay, API key, subscription login and supported
  desktop transition without moving credentials into Runtime.
- Freeze exact evidence and UI terms for each supported relationship and all incompatibilities.
- Include version drift, missing transcript, corrupt session, cross-host and cross-scope cases.

## Acceptance

The matrix proves or rejects each relationship with provider/session/workspace evidence. No handshake,
matching prose or copied session string is accepted as proof of exact resume.

## Makes deletable

Assumptions that every Claude surface can continue every Agent SDK session.
