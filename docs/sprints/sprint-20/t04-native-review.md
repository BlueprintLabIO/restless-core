# S20-T4 — Integrate and live-probe the research publication

**Layer:** Runtime + owner surface.

Build every public route from a clean install and inspect the exact staged candidate at desktop and
mobile. The visual direction must stay within the accepted candidate A identity and make long-form
evidence easier to read.

**Deletion target:** parallel page variants and a second visual system.

## Current evidence and remaining gate

The staged candidate passed `npm run verify` and explicit HTTP probes for its product, research,
corpus, journal, comparison and legacy-redirect routes on 26 August 2026. The source replacement also
removed the old bridge/matrix components and findings collection. A connected rendered desktop/mobile
browser was unavailable in this environment, so the required native visual inspection remains open;
this ticket is intentionally not checked.
