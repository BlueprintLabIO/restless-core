# S40-T0 — Freeze the Codex takeover corpus

**Layer:** Full product evaluation  
**Serves:** Exact native takeover must be judged by company continuity, not whether a terminal opened.

## Work

- Freeze one hard-goal Attention case and one active productive Attempt at idle, active-turn,
  tool-running, terminal, cancelled and failed boundaries.
- Record expected company/actor/Work/attempt/conversation, App Server thread/session, cwd, revision,
  model, control owner, checkpoint, handle and return states.
- Add adversarial wrong-company, wrong-actor, wrong-thread, duplicate-control, expired-handle,
  disconnect, Runtime-restart, no-change and unobservable-change cases.
- Freeze the owner-language rubric for observing, taking control, uncertain control and returning.

## Acceptance

Every case names the native evidence, canonical Restless change, workspace observation, cleanup result
and prohibited false claim. The corpus can fail before a real model call where identity or authority is
wrong and uses only dedicated `_test` companies for destructive cases.

## Makes deletable

Ad hoc terminal demos and acceptance based only on a matching repository path.
