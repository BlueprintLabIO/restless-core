# S14-T0 — Freeze hardening contract and verification baseline

**Layer:** Cross-layer and evaluation.

**Observed friction served:** S12 now has enough outcome evidence to make invasive cleanup useful, but
its remaining owner-browser gate and previously silent DB skips make an unbounded refactor dangerous.

## Outcome

The S14 safety/evidence contract is frozen, the current Rust and web baseline is recorded, and the
unstarted S13 TypeScript/OIDC experiment is removed before it becomes a competing architecture.

## Acceptance

- Link the exact S12 run-report observations that motivate the meter lane and source-worktree guard.
- Record the baseline commands and whether each proves format, unit behaviour, live-Postgres behaviour,
  full workspace behaviour, web behaviour, or Runtime connectivity.
- State the only remaining S12 gate: connected desktop/mobile owner-surface review.
- Confirm S14 performs no TypeScript control-plane migration; remove `experiment/sprint-13/` and revise
  the unstarted S13 draft to an archived decision record.
- Freeze the no-new-entity constraints for metering, review preparation, verification and module moves.
- Preserve unrelated dirty worktree changes.

## Deletion target

The generic TypeScript/OIDC premise, its isolated package scaffold, and any claim that Sprint 12 is
fully visually signed off.
